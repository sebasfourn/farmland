<div class="notification-container <%= Message.unseen_messages_count_for(current_user) > 0 ? "" : "d-none" %>">
    <div class="notification-dot">
      <div class="notification-count">
        <%= Message.unseen_messages_count_for(current_user) %>
      </div>
    </div>
  <%= audio_tag asset_path("cowbell.mp3"), id: "notificationAudio", control: true%>
</div>
